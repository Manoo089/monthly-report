name: updates
on:
  schedule:
    - cron: '40 12 * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call our API route
        run:
          https://monthly-report.vercel.app/api/'